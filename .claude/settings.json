{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && git status --porcelain | grep -q . && git add -A && git commit -m \"Auto-commit: $(date '+%Y-%m-%d %H:%M')\" && git push origin main 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "변경사항 자동 커밋 중..."
          }
        ]
      }
    ]
  }
}
